<h1>Toby Alden</h1>

<p>The personal portfolio of Toby Alden.</p>

<ul>
	<% @categories.each do |category| %>
		<li> <%= link_to category.name, category_path(category) %>  </li>
	<% end %>
</ul>

<% if is_admin? %>
	<p> <%= link_to 'New Category', new_category_path %> </p>
<% end %>
